[project]
name = "clone-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JavohirBoboxonov", email = "javohirboboxonov08@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
clone-project = "clone_project:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"